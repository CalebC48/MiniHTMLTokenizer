# MiniHTMLTokenizer
The goal of this project is to build a small, standalone HTML tokenizer implemented in C++. The tokenizer reads an input file and produces a stream of tokens representing HTML constructs such as tags, attributes, text, comments, and doctype declarations.
